from enum import Enum, auto

#TODO: horrendus name of class, 
class ExtractionMethod(Enum):
    """Enum representing different PDF extraction methods."""
    STANDARD = "standard"
    CRUD = "crud"
    
